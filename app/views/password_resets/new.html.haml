= simple_form_for @user, url: password_resets_path, wrapper: :horizontal, html: { class: 'form-horizontal' } do |f|
  .fieldset
    %legend
      Forgot Password
    = f.input :email
    .form_actions
      = f.submit 'Send Mail', class: 'btn btn-primary btn-lg btn-wide'
