- if flash.any?
  .row-fluid
    .span12
      - flash.each do |name, message|
        .alert{:class => (name == :notice ? 'alert alert-success' : 'alert alert-error')}
          %a.close{'data-dismiss' => 'alert'}×
          - if name == :notice
            %i.icon-info-sign
          - else
            %i.icon-exclamation-sign
          = message
