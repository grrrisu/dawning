// Generated by CoffeeScript 1.9.2
(function() {
  var bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Game.SpritePool = (function() {
    function SpritePool(images) {
      this.returnSprite = bind(this.returnSprite, this);
      this.getSprite = bind(this.getSprite, this);
      this.createSprite = bind(this.createSprite, this);
      this.sprites = {};
      images.each((function(_this) {
        return function(image) {
          return _this.sprites[image] = [];
        };
      })(this));
    }

    SpritePool.prototype.createSprite = function(image) {
      var texture;
      texture = PIXI.Texture.fromImage(image);
      return new PIXI.Sprite(texture);
    };

    SpritePool.prototype.getSprite = function(image) {
      if (this.sprites[image].length === 0) {
        return this.createSprite(image);
      } else {
        return this.sprites[image].shift();
      }
    };

    SpritePool.prototype.returnSprite = function(sprite, image) {
      return this.sprites[image].push(sprite);
    };

    return SpritePool;

  })();

}).call(this);
